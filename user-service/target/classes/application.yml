server:
    port: 8080

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/oms?useSSL=false&serverTimezone=UTC
    username: root
    password: 430103535
    driver-class-name: com.mysql.cj.jdbc.Driver

mybatis:
  mapper-locations: classpath:mapper/*.xml
  type-aliases-package: com.OMS.user_service.pojo
  configuration:
    map-underscore-to-camel-case: true

logging:
  level:
    root: INFO
    org.springframework: DEBUG
    com.zaxxer.hikari: DEBUG
    org.mybatis: DEBUG
